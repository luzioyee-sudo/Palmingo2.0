kind = "web"
previewPath = "/"
title = "Palmingo"
version = "1.0.0"
id = "artifacts/palmingo"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 20088

[services.development]
run = "pnpm --filter @workspace/palmingo run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/palmingo", "run", "build" ]
publicDir = "artifacts/palmingo/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "20088"
BASE_PATH = "/"
